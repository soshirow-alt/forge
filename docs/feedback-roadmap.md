# フィードバック — 将来方針（未実装メモ）

この文書は実装予定の設計メモです。オーナー合意済みの方向性を Cursor が実装時に参照します。

## 優先順位（2026-06 時点）

1. フィードバック表示改善 — **実装済み**
2. 開発者ダッシュボード FB 一覧 — **実装済み**
3. 開発フェーズ表現の整理 — 未着手
4. バージョン別フィードバック仕様 — 未着手（migration 004）

## バージョン別フィードバック（将来）

Forge の将来コア機能として採用。

### 基本ルール

- 1ユーザー × 1作品 × 1プレイ可能バージョン = 1件
- 同一バージョン内では編集可能
- 新版公開で新規投稿枠が開く（旧版 FB は履歴として残る）

### バージョン表記

- Forge が勝手に固定しない
- **開発者自由入力**を基本
- 理想：将来「Forge 自動採番 / 開発者手動入力」を選べる

### 技術イメージ（migration 004 時）

- `projects.playable_version`（text）
- `project_feedback.version_key`（投稿時スナップショット）
- UNIQUE(user_id, project_id, version_key)
- UPDATE ポリシー（自分の FB のみ）
- 版 bump：開発ログ投稿時「新しいプレイ可能版」チェック（推奨）または作品編集

### 今回やらないもの

- AI 要約
- 開発者返信
- 通知強化（版公開時の watch 通知等は Phase 2 以降）
