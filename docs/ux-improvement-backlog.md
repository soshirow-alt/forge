# UX 改善バックログ — オーナーレビュー反映

**日付**: 2026-06-12（プレイヤーサイクル正式定義追記）  
**前提**: migration 004 適用・deploy 済み。**コアループは原典どおり最初から正しく** 設計するフェーズへ移行。  
**方針**: 周辺機能（SDK・閲覧数等）は段階的。声を届ける / 変化を見る / 再プレイはコア。

---

## プレイヤーサイクル — 実装バックログ（原典 GO 2026-06-12 · 実装進行中）

**原典**: `docs/forge-principles.md` §1・§5・§7・§8

| 順 | 項目 | 内容 | 状態 |
|----|------|------|------|
| **1** | migration 006 | `project_version_prompts` + `project_voice_responses` + RLS + 公開集計 RPC | コード済・**本番適用待ち** |
| **1b** | 開発者問い設定 UI | `/submit` + `/projects/{id}/edit` | **実装済** |
| **2** | 1 段目 UI（初声） | 複数問い・1 問 OK・返事を届ける | 進行中 |
| **3** | 2 段目統合 | 深い改善材料を折りたたみ任意 | 進行中 |
| **4** | みんなの声（プレイヤー） | 集計グラフのみ（個別非公開） | 進行中 |
| **5** | みんなの声（開発者） | 問い別集計 + ルールベース解釈（AI は後段） | 進行中 |
| **6** | 変化を見る | devlog / 新版 / 参考にした声 | 一部実装 |
| **7** | Phase A deploy | プレイ→初声の橋 | コードあり |

**非コア（後回し可）**: SDK、プレイ人数、閲覧数

---

## オーナー判断（2026-06-12 確定）

| 項目 | 判断 |
|---|---|
| テスター募集中タブ | 短期維持。中長期は廃止/統合寄り（新着・人気・最近更新など） |
| P0 CTA | 十分。新規 page 不要 |
| 名称 | **開発ダッシュボード** |
| 実装 | P0 完了 → UX 再レビュー後 P1 判断 |

---

## オーナー所感（要約）

> 機能不足より「何をすればいいのか分からない」が強い。  
> 技術問題より UX 問題。新機能追加より UX 整理フェーズ。

---

## 課題マップ

| ID | テーマ | オーナー指摘 | 分類 |
|---|---|---|---|
| UX-1 | 開発者導線 | 投稿後の次アクション不明。devlog が探しにくい。作品詳細＝プレイヤー向けに見える | **構造** |
| UX-2 | devlog フォーム | 「タイトル」が何のタイトルか不明 | **低コスト** |
| UX-3 | ホームタブ | 「新着」と「テストプレイ受付中」の違いが直感的でない | **構造（＋低コスト copy）** |
| UX-4 | ログイン | Enter でログインできない | **低コスト**（要再現確認） |
| UX-5 | 全体 | ラベル・説明・画面構成 | 上記に集約 |

---

## 低コスト vs 構造的問題

### 低コスト（数時間〜1日、既存画面の copy / 小 UI 変更）

| 項目 | 内容 | 根拠 |
|---|---|---|
| **UX-2** devlog ラベル | 「タイトル」→「今回の更新タイトル」。placeholder・補足文を追加（例：「プレイヤーに伝える今回の変更点の見出し」） | `devlog-new-page.tsx` の label 変更のみ |
| **UX-4** ログイン Enter | 本番で再現確認。`login-page.tsx` は既に `<form onSubmit>` あり → 再現すれば `password` の Enter ハンドラ or フォーム構造の微修正 | 1 ファイル |
| **投稿完了画面** | 成功後 CTA を「次にやること」順に：`開発ログを書く`（primary）→ 作品詳細 → ダッシュボード | `submit-page.tsx` の link 順・文言 |
| **my-projects 行アクション** | 各作品に `[開発ログを書く]` `[FBを見る]` を表の操作列に追加（新 route 不要） | `my-projects-page.tsx` |
| **ホームタブ subtitle 強化** | 既存 subtitle の見せ方改善、タブ下に 1 行説明を常時表示 | `home-page.tsx` copy のみ |
| **004 関連 copy** | playable_version 表示位置の改善は copy+1 行追加レベル（概要欄に現行版） | 前回 verification より |

### 構造的問題（設計判断＋複数画面・新 route の可能性）

| 項目 | 内容 | なぜ構造か |
|---|---|---|
| **UX-1** 作品ダッシュボード | 作品単位の「育てる場所」がない。`/games/[id]` はプレイヤー向け UI が主 | 新 page `/projects/[id]` or dashboard 相当、ナビモデル変更 |
| **UX-1** 開発者 vs プレイヤー画面分離 | オーナーは「作品詳細」ではなく「作品ダッシュボード」を期待 | IA（情報設計）の再定義 |
| **UX-3** ホーム discovery タブ | 「新着」「テストプレイ受付中」の意味が Forge 文脈とズレ | タブ定義・フィルタロジック・投稿フォーム `lookingForTesters` との一貫性 |
| **ダッシュボード全体** | `/my-projects` は作品一覧表＋全体 FB inbox。作品別 stats・次アクションが弱い | ページ目的の再設計 |

---

## 現状の実装メモ（Cursor 確認）

| 画面 | 現状 |
|---|---|
| 投稿成功 | 「ホーム」「作品を見る」「ダッシュボード」— **devlog 導線なし** |
| devlog 入口 | 作品詳細 → 開発の歩み → 小さな「開発ログを投稿」リンクのみ |
| ダッシュボード | 作品表＋全体 FB inbox。作品行は「詳細」「編集」「削除」 |
| ホーム | タブ: 新着作品 / テストプレイ受付中 / 急上昇。testers = `lookingForTesters` フィルタ |
| ログイン | form onSubmit 実装済み。**Enter 不可は要再現**（IME・disabled 等の可能性） |

---

## 優先順位（Cursor 提案）

### P0 — MVP として今すぐ（次スプリント）

| 順 | 項目 | 理由 |
|---|---|---|
| 1 | **UX-2** devlog ラベル・説明 | コアループ「改善」の入口。迷いが直接 devlog 投稿を止める |
| 2 | **投稿完了 → 次アクション** | 投稿直後が最も「次に何を？」が発生する瞬間 |
| 3 | **my-projects に作品別 CTA** | 新 page なしで UX-1 の 60% をカバー |
| 4 | **UX-4** ログイン Enter | 毎セッションの摩擦。低コスト |

### P1 — MVP 直後（軽い設計判断を含む）

| 順 | 項目 | 理由 |
|---|---|---|
| 5 | **UX-3** ホームタブ copy 再整理 | プレイヤー初回体験。まず説明改善、効果見てタブ統合を判断 |
| 6 | **playable_version / 新 FB 導線 copy** | 004 コアループの「次の FB」が分かりにくい（前回 verification） |
| 7 | **オーナー向け作品詳細バナー** | オーナーが `/games/[id]` を開いたとき「開発者メニュー」へ誘導 |

### P2 — 後回しでよい（構造変更・产品判断待ち）

| 項目 | 理由 |
|---|---|
| **作品単位ダッシュボード新設** | 効果大だが route・UI 新規。P0/P1 で様子見 |
| **ホームタブ統合・廃止** | `lookingForTesters` の产品価値をオーナーと再定義してから |
| **開発者/プレイヤー画面完全分離** | P1 バナー＋CTA で足りるか検証後 |
| **アナリティクス / SDK セクション** | 既に Coming Soon。MVP 外 |
| **旧版 FB プレイヤー表示** | 004 で意図的に後回し |

---

## Forge MVP として「今直すべきもの」

**テーマ**: 「何をすればいいか」を **言葉とボタン** で答える（新機能なし）

1. **開発者の次アクションを明示**
   - 投稿完了：「① 開発ログを書いて改善を共有 → ② プレイヤーの FB を見る」
   - ダッシュボード：作品ごとに `[開発ログを書く]` `[FBを見る]` `[編集]`
2. **devlog フォームのラベル・説明**
   - 何を書く画面か、各項目が何を指すか
3. **ログイン Enter**（再現確認のうえ修正）
4. **ホームタブ** — 最低 1 行の説明追加（統合は P2）

**狙い**: オーナーが触って感じた「探す・迷う」を、**画面追加なしで半分減らす**。

---

## 後回しでよいもの

- 作品ダッシュボード **新規ページ**（P0/P1 の CTA 改善後に判断）
- ホーム **タブ構造の抜本変更**
- プレイヤー/開発者 **完全別 URL 体系**
- 通知強化・版 bump バナー（004 UX 改善の深掘り）
- アナリティクス実装

---

## オーナー判断を仰ぎたい点（実装前）

1. **ホーム「テストプレイ受付中」** — 残す？ 名前変更？ 新着に統合？
2. **作品ダッシュボード** — P0 の CTA 追加で足りるか、早期に `/projects/[id]/manage` を作るか
3. **「ダッシュボード」名称** — ヘッダーは「ダッシュボード」→ `/my-projects`。作品単位 dashboard との用語整理

---

## 関連ドキュメント

- `docs/forge-principles.md` — **プレイヤーサイクル正式定義**（§1・§5・§8）
- `docs/migration-004-verification-report.md` — 004 実地確認・UX ギャップ
- `docs/feedback-roadmap.md` — FB 版管理の将来方針
