-- =============================================================================
-- Step 2 — Migration 070 (Production manual apply)
-- =============================================================================
-- Dashboard貼付元（正本）:
--   supabase/migrations/070_age_rating_feedback_engagement.sql
--
-- 手順:
--   1. リポジトリで上記ファイルを開く
--   2. ファイル全文をこの SQL Editor に貼付
--   3. Run → 成功を確認してから 071 へ
--
-- 含めないこと:
--   scripts/staging-only/sql/sync-project-watches-authenticated-select.sql
-- =============================================================================

SELECT 'Paste full contents of supabase/migrations/070_age_rating_feedback_engagement.sql' AS instruction;
